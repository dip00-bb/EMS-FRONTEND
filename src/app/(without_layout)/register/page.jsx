import useAuth from '@/app/Hooks/useAuth';
import React from 'react';

const Rd = () => {
    // const {user}=useAuth()
    return (
        <div>
            
        </div>
    );
};

export default Rd;